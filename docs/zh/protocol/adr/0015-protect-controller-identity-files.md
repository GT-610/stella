# ADR 0015：使用原生 ACL 保护控制器身份文件

- 状态：已接受
- 日期：2026-08-30

## 决策

控制器 Ed25519 私钥和 TLS 私钥以未加密 PKCS#8 文件保存，但目录和文件的 DACL 会先禁用
继承，仅授予运行控制器的账户和 LocalSystem 所需访问。创建使用临时文件、显式权限和原子
重命名；无法建立或验证保护时关闭并删除新文件。

## 后果

无人值守的 Windows 控制器可加载长期身份，同时其他本地账户不能通过 DACL 读取或替换密钥。
跨账户迁移需要显式的权限加固操作。
