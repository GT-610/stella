# ADR 0013：在专用 crate 中共享控制通道机制

- 状态：已接受
- 日期：2026-08-30

## 决策

控制器和客户端共用 `stella-control` crate，其中包含有界记录读写、消息构建、序列验证、
关联追踪和 TLS 导出器证明转录。该 crate 不拥有套接字、TLS 信任策略、授权策略或持久状态，
线上验证委托给 `stella-proto`，密码操作委托给 `stella-crypto`。

## 后果

双方以同一方式处理分帧、序列和证明输入，减少协议实现分歧。工作区依赖略增，但异步 I/O
不会进入协议或密码学 crate。
