# ADR 0005：将实现组织为 Cargo 工作区

- 状态：已接受
- 日期：2026-08-29

## 背景

协议解析、密码学、传输、TAP 访问、控制器行为和客户端编排具有不同安全边界和测试需求。

## 决策

使用一个 Cargo 工作区，包含职责狭窄的协议、TAP、传输、密码学和共享类型库 crate，另有
独立的服务器与客户端二进制文件。依赖版本和 lint 在工作区根目录管理，crate 不得形成
依赖环。

## 后果

平台代码和纯解析逻辑可独立编译与测试。工作区会增加清单数量，但依赖方向和公开 API
所有权保持可见。
