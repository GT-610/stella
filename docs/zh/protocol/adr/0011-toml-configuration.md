# ADR 0011：使用 TOML 进行本地配置

- 状态：已接受
- 日期：2026-08-29

## 决策

参考实现使用由 Serde 解码的 UTF-8 TOML 文件。未知字段是错误。命令行可选择文件并覆盖一小
组已记录的运行值，但不会形成第二套配置模式。长期身份密钥、TLS 私钥和可复用加入秘密存放
在单独文件或操作系统凭据设施中，只通过路径引用；Windows 设置会验证秘密文件不被广泛
写入，并在无法验证 ACL 时警告。

## 后果

部署使用一种已记录且严格验证的配置语法。秘密配置仍是独立运行问题，未来模式更改需要
显式迁移文档。
