# ADR 0018：限制控制器运行时接纳与关闭

- 状态：已接受
- 日期：2026-08-30

## 决策

控制器在监听、TLS 握手、未认证连接、已认证会话、请求执行、响应写入和关闭等待处都使用
明确上限与期限。信号量限制同时接受的控制连接；慢 TLS 客户端、无效记录和未认证连接在
期限到达时关闭。关闭停止接纳新连接，向活动会话发送有界排空期限，随后取消剩余任务并
有序停止授权线程。

## 后果

连接和任务增长有固定上界，慢对等方不能耗尽应用内存或无限阻塞服务管理。超过限制的工作
由操作系统监听积压或失败路径处理。
