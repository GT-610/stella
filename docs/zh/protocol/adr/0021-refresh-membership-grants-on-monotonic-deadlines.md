# ADR 0021：按单调期限刷新成员授权

- 状态：已接受
- 日期：2026-08-30

## 决策

控制器在发送加入快照、请求快照、协调快照或授权刷新后，解码准确的本地授权，并在其
有效期中点安排下一次刷新。延迟用 Tokio 单调 `Instant` 表示；仅在签发替换授权时再次
读取 Unix 时间。顺序会话循环在下一个请求、控制器关闭和最早刷新期限之间选择，并以
关联零发送 `GRANT_REFRESH`。

## 后果

授权有效性不依赖心跳时间，墙钟回拨不能推迟已计划的进程内期限。进程挂起或时钟跳变仍
可能导致刷新过晚，客户端在最后有效授权过期后继续失败关闭。
