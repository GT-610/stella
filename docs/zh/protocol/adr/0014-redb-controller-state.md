# ADR 0014：在 redb 中存储控制器授权状态

- 状态：已接受
- 日期：2026-08-30

## 决策

控制器将网络、节点、成员关系、令牌摘要、端点租约、epoch 与快照修订保存在单一 redb
数据库中。一个专用阻塞授权线程拥有数据库，并以串行化类型化命令处理读写。事务在提交前
检查跨记录不变量；初始化、备份和验证都通过该边界执行。

## 后果

持久状态拥有崩溃可见的原子性与一致快照。控制器保持单个可部署可执行文件和一个状态文件，
但数据库访问需经过授权线程。
