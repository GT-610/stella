# ADR 0017：持久化带端点集合的对等租约

- 状态：已接受
- 日期：2026-08-30

## 决策

控制器将在线 `(network_id, node_id)` 对的端点集合保存为有过期时间的租约。端点记录包含
网络和节点 ID、启用状态、活动成员关系及规范端点序列。心跳只刷新该连接已加入网络的现有
租约；删除、禁用、离开和到期会移除租约并推进相关快照修订。

## 后果

控制器能区分已授权成员与在线成员，并向快照提供当前端点。畸形或孤立租约状态会阻止
控制器启动，而不是向客户端发布不一致的对等状态。
