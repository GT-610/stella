# 4. 网络模型

控制器创建虚拟网络、管理成员关系并签发策略；它不学习节点间的源 MAC 位置。每个节点在
已认证数据流量上维护自己的转发表。网络 ID 是所有可变数据平面对象的首要键，避免不同
虚拟网络之间的会话、MAC、策略或端点混淆。

节点只在拥有有效本地授权、匹配策略和活动对等会话时转发。已知单播使用学习到的对等节点；
广播、多播和未知单播按策略复制。MAC 迁移与冲突有明确的年龄、争用和抑制规则，防止短暂
变化或攻击者流量无限改写转发表。
