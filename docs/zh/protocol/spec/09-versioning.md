# 9. 版本控制

协议版本由主版本、次版本和密码学套件组成。控制器在 `SERVER_HELLO` 中按偏好顺序公布其
支持的元组，客户端从双方都支持的元组中选择一个。0.1 不允许实现自定义算法选择或在协商
失败时回退到不兼容版本。

注册表为数据包类型、控制字段、扩展、状态代码和其他线上分配保留稳定含义。未知非关键
字段可在完整边界验证后跳过；未知关键字段必须拒绝整个包或消息。破坏既有字节语义的变化
需要新的主版本或明确迁移，不得重定义旧版本的已分配含义。
