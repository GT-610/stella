# 1. 线上格式

本章定义 Stella 数据报包络、保护数据包、分片、扩展和重放相关字段。所有多字节整数使用
大端字节序；线上结构独立于实现语言的内存布局。接收方先验证魔数、版本、类型、长度、
标志、保留字节和扩展边界，再解析和认证负载。

数据平面将完整以太网帧（不含 FCS）放入受保护数据包。每个方向有独立会话 ID、密钥、nonce
前缀和递增序列空间。超过路径大小的帧以同一逻辑帧 ID 分片，接收方只在所有片段通过认证且
在有界重组期限内到齐时交付。未知关键扩展、非零保留字节、长度不一致、重复片段和重放包
必须拒绝。
