# 3. 身份

Stella 使用控制器 ID、节点 ID 和网络 ID 三类 16 字节标识符。节点和控制器 ID 分别从域分隔的
Ed25519 公钥散列派生；网络 ID 标识控制器管理的独立二层域。私钥不穿过网络，节点在本地用
操作系统随机数生成 Ed25519 密钥对。

注册将未知节点公钥与控制器管理的显示名关联，注册令牌是一次性 Bearer 凭据。成员授权由
控制器签名，绑定节点、网络、权限、策略摘要、epoch 和有效时间。撤销、暂停、删除成员或
推进 epoch 会使旧授权和会话失效。实现应保护私钥文件，备份同一私钥会保留同一节点身份。
