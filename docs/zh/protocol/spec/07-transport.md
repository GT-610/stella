# 7. 传输

Stella 依赖承载完整数据报的可替换传输契约。0.1 规定 UDP 后端：实现使用数值端点、正常 UDP
校验和、有界单包发送和接收，并拒绝截断数据报而不是解析可见前缀。传输不解释以太网、
不选择泛洪接收方，也不提供可靠字节流。

每个 UDP 套接字绑定一个明确 IPv4 或 IPv6 地址。路径数据报大小受配置和协议上限控制；发送
到错误地址族、超出限制或部分发送均为错误。客户端在 TAP 分类、加密封装和 UDP I/O 间使用
有界队列，取消和关闭必须唤醒待处理操作并释放套接字资源。
