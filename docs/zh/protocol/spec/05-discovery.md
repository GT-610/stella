# 5. 发现

控制器向已认证成员分发当前对等节点的数值 IP/UDP 端点集合及优先级。端点是投递候选项，
不是路径授权或身份声明。客户端只接受来自控制器快照的端点，并在签名握手完成后将会话固定
到精确源地址和端口。

端点租约通过发布和心跳维持，到期、离开、撤销或控制器状态变更时移除。握手、保活和错误
处理均有速率和时间边界。底层可能丢包、重复、重排或延迟；Stella 数据投递保持数据报尽力
而为语义，不以端点发现承诺可靠传输或 NAT 穿透。
